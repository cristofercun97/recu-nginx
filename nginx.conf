server{
    listen 80;
    sever_name localhost;
    location/{
        root  /usr/share/nginx/html;
        index index.html ; 
    }
    location/assets/{
        root /usr/share/nginx/assets; 
    }
}